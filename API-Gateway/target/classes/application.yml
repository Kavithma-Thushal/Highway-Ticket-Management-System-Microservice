spring:
  application:
    name: Api-Gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: User-Service
            uri: lb://User-Service
            predicates:
              - Path=/api/v1/user/**

server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/